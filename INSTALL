= Requirements
* autoconf 2.61 or higher
* automake 1.10 or higher
* gcc 4.2.1 or higher
* GNU Make 3.81 or higher
* libxml2 2.7.3 or higher

= After getting HEAD
For impatient people with the source at /path/to/clibsedml:
$ cd /path/to/clibsedml
$ autoreconf -i -f
$ ./configure
$ make all
$ make check
Or, we recommend the following out-of-tree style if you have some time
and /tmp available:
$ cd /path/to/clibsedml
$ autoreconf -i -f
$ mkdir /tmp/clibsedml
$ cd /tmp/clibsedml
$ /path/to/clibsedml/configure
$ make all
$ make check
