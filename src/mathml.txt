/* token */
MATHML(CN),
MATHML(CI),
MATHML(CSYMBOL),
MATHML(SEP),
/* general */
MATHML(APPLY),
MATHML(PIECEWISE),
MATHML(PIECE),
MATHML(OTHERWISE),
MATHML(LAMBDA),
/* relational operators */
MATHML(EQ),
MATHML(NEQ),
MATHML(GT),
MATHML(LT),
MATHML(GEQ),
MATHML(LEQ),
/* arithmetic operators */
MATHML(PLUS),
MATHML(MINUS),
MATHML(TIMES),
MATHML(DIVIDE),
MATHML(POWER),
MATHML(ROOT),
MATHML(ABS),
MATHML(EXP),
MATHML(LN),
MATHML(LOG),
MATHML(FLOOR),
MATHML(CEILING),
MATHML(FACTORIAL),
/* logical operators */
MATHML(AND),
MATHML(OR),
MATHML(XOR),
MATHML(NOT),
/* quantifiers */
MATHML(DEGREE),
MATHML(BVAR),
MATHML(LOGBASE),
/* trigonometric operators */
MATHML(SIN),
MATHML(COS),
MATHML(TAN),
MATHML(SEC),
MATHML(CSC),
MATHML(COT),
MATHML(SINH),
MATHML(COSH),
MATHML(TANH),
MATHML(SECH),
MATHML(CSCH),
MATHML(COTH),
MATHML(ARCSIN),
MATHML(ARCCOS),
MATHML(ARCTAN),
MATHML(ARCSEC),
MATHML(ARCCSC),
MATHML(ARCCOT),
MATHML(ARCSINH),
MATHML(ARCCOSH),
MATHML(ARCTANH),
MATHML(ARCSECH),
MATHML(ARCCSCH),
MATHML(ARCCOTH),
/* constants */
MATHML(TRUE),
MATHML(FALSE),
MATHML(NOTANUMBER),
MATHML(PI),
MATHML(INFINITY),
MATHML(EXPONENTIALE),
/* MathML annotations */
MATHML(SEMANTICS),
MATHML(ANNOTATION),
MATHML(ANNOTATION_XML),
